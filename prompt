یک گزارش متنی از ایراداتی که در دریافت دیتا توسط سرویس ها و تحلیل و تولید سیگنال وجود دارد بهم تحویل بده.
همچنین راه حل و بخشی که باید اصلاح شود را نیز در گزارش بیان کن.
نمونه کد نمیخوام و فقط گزارش متنی باشه.
تمرکزت فقط روی گزارش ایرادات منطق تولید سیگنال باشه!








با توجه به گزارش زیر ایرادهای پروژه را برطرف کن.

با توجه به لاگ برنامه در فایل app.log خطاهای پروژه را برطرف کن.

از کامنت در کد استفاده نکن.
فقط کلاس هایی که ویرایش کردی را در فایل های جداگانه ارسال کن تا کپی راحت تر باشد.